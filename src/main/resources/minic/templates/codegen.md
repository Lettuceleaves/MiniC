<!-- 代码生成（汇编）阶段说明模板 -->
<!-- 本文件定义 codegen/ASM 阶段各指令类型的解释文本 -->
<!-- 格式: ## 指令类别 下面是该类别的解释正文 -->
<!-- 加载时忽略 HTML 注释行 -->

## header
汇编阶段把 IR 翻译成 Windows x64 汇编。当前第 ${lineNumber} 行，类型 `${kind}`，段 `${section}`，标签 `${label}`，文本为 `${text}`。

## footer
用途: 汇编是生成 exe 前最接近机器执行的表示。看这一行可以知道源码/IR 最终变成了寄存器移动、算术、比较、跳转、函数调用还是返回。

## label
标签是汇编中的跳转目标，不直接执行计算。条件分支、循环和函数入口会跳到这些名字。

## mov
mov 负责复制数据，例如把立即数、寄存器值或内存值移动到目标寄存器/内存。

## arithmetic
算术指令执行整数计算，通常对应源码里的加减乘或栈指针调整。

## compare
cmp/test 设置 CPU 标志位，后续条件跳转会根据这些标志决定走哪条路径。

## jump
j* 是跳转指令。无条件跳转用于连接基本块，条件跳转用于 if/循环判断。

## call
call 调用函数。Windows x64 下前几个参数通常放在 rcx、rdx、r8、r9，返回值通常在 rax。

## ret
ret 从当前函数返回到调用者，结束当前函数的机器级执行。

## stack
push/pop 操作栈，用于保存/恢复寄存器或维护调用帧。

## default
该汇编行是最终交给汇编器的低层指令或伪指令，直接决定生成的目标文件内容。
