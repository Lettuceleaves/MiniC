<!-- IR 降级阶段说明模板 -->
<!-- 本文件定义 IR 阶段各指令类型的解释文本 -->
<!-- 格式: ## 指令类别 下面是该类别的解释正文 -->
<!-- 加载时忽略 HTML 注释行 -->

## header
IR 阶段把 AST 降成更线性的中间表示。当前是第 ${lineNumber} 行，文本为 `${text}`。

## footer
用途: IR 抹平了很多语法细节，让后端可以按顺序生成汇编。结合左侧源码范围和这一行文本，可以看出某个 AST 节点最终产生了哪一步计算、跳转、调用或返回。

## call
call 表示函数调用。它把已经求值的参数交给目标函数，并把返回值保存到临时值或忽略。

## return
return/ret 表示当前函数结束并返回结果，后端会把返回值放到平台约定的位置。

## branch
br/jump 表示控制流跳转，用来连接 if、for、while 这类结构产生的基本块。

## compare
比较类 IR 会计算条件值，后续通常接条件跳转，用于分支和循环判断。

## store
赋值/存储类 IR 把右侧计算结果写入变量、临时值或内存位置，是变量状态变化的来源。

## load
load 表示读取变量或内存中的值，后续计算使用的是读取出的临时值。

## default
该 IR 行是后端前的中间表示，把 AST 中较复杂的语法结构拆成更接近机器执行顺序的小步骤。
